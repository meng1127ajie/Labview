# JustFloat Studio HSS硬件验收清单

当前软件版本：1.5.0  
目标：STM32G431RB / SWD / J-Link HSS  
当前状态：测试工程模拟链路通过；正式工作台仅支持真实HSS，硬件待验证。

## 验收前准备

- [ ] 确认板上固件与所选ELF来自同一次编译。
- [ ] ELF保留符号表和DWARF调试信息，未strip。
- [ ] 准备至少两个地址稳定的 `volatile` 标量变量。
- [ ] 关闭IDE调试会话、J-Link GDB Server、J-Scope和其他占用同一探针的软件。
- [ ] 确认设备选择为 `STM32G431RB`、接口SWD、速度先从4000 kHz开始。

## 基础连接与能力

- [ ] “检查环境”显示V9.52 DLL所需HSS导出全部存在。
- [ ] 点击连接后 `JLINK_Open/TIF_Select/Connect` 成功。
- [ ] 记录 `JLINK_HSS_GetCaps`：
  - MaxBlocks：________
  - MaxFrequencyHz：________
  - RawCapabilities：0x________
- [ ] 软件能拒绝超过MaxBlocks或MaxFrequency的配置，并显示明确原因。

## 采样正确性

- [ ] 先以100 Hz选择1个Float32变量，波形和值与固件/J-Scope一致。
- [ ] 增加到2～4个变量，确认变量顺序和类型解码正确。
- [ ] 提升到500 Hz、1 kHz，记录实际/平均采样率。
- [ ] HSS时间戳相邻差值与配置周期一致，无倒退。
- [ ] 暂停显示后后台采集继续，继续显示可回到最新数据。
- [ ] 双游标的 `Δt` 与采样点数×周期一致，`ΔY=B-A`正确。

## 运行状态变化

- [ ] CPU运行时HSS稳定采样，不影响电机控制实时性。
- [ ] CPU Halt时HSS暂停，Continue后自动恢复。
- [ ] 断开按钮调用 `JLINK_HSS_Stop` 并关闭DLL连接。
- [ ] 从启动中心确认安全退出后，不残留JustFloat Studio/J-Link占用。
- [ ] 重新连接RTT模式成功，证明HSS已释放探针。

## 压力和异常

- [ ] 最大变量数连续采样10分钟，无界面卡顿、内存无限增长或时间戳错位。
- [ ] 拔掉J-Link/目标掉电时显示清晰错误，不崩溃。
- [ ] ELF与固件不匹配时能通过异常数值识别，并停止测试更换正确ELF。
- [ ] IDE占用探针时连接失败信息明确，不尝试强杀外部IDE进程。

## 验收结论

- [ ] 通过
- [ ] 有条件通过，限制：________________________________
- [ ] 未通过，问题：____________________________________

测试日期：____________  测试人：____________  J-Link型号：____________
